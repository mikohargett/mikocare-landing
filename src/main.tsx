import React from 'react';
import ReactDOM from 'react-dom/client';
import MikoCareLanding from './MikoCareLanding';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<MikoCareLanding />);
